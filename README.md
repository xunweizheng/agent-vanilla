# agent-vanilla

Empty base repo for **Cursor Cloud Agents** used by Grok Bot.

- Purpose: satisfy Cloud Agent \"must bind a repository / environment\" only.
- Keep empty: short README + Cursor environment config only. No notes, no project code.
- Temporary work: use short-lived branches, delete when done.

Not for personal docs (see other repos).
